# Backend API base URL (no trailing slash, no /api suffix — that's added automatically).
# Defaults to http://localhost:8080 if unset, matching the backend's local dev default.
VITE_API_URL=http://localhost:8080
